import React from "react";
import { Routes, Route } from "react-router-dom";

const GameRouter = () => {
  return (
    <Routes>
      <Route path="/" />
    </Routes>
  );
};

export default GameRouter;
